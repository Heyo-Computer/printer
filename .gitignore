# Rust build artifacts
target/
**/target/

# Atomic-write temp files left behind by `printer task` if a write was
# interrupted mid-rename
**/*.md.tmp

# codegraph index (not source of truth, rebuild with `codegraph index`)
.codegraph/

# OS / editor cruft
.DS_Store
