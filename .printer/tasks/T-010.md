+++
id = "T-010"
title = "new command to create an agent session to generate a new spec from the follup file"
status = "done"
priority = 3
created_at = "2026-04-29T03:34:25Z"
updated_at = "2026-04-29T03:43:33Z"
owner = "sarocu"
labels = []
depends_on = ["T-009", "T-011"]
blocked_reason = ""
spec_anchor = "c25816962d3a515d"
+++
## Notes

- 2026-04-29T03:36:25Z [sarocu] Plan:
- Goal: new subcommand that spawns one agent turn to convert a saved followups file (from T-009) into a new spec markdown in the canonical printer format, written into specs/NNN-<name>.md (per T-011's numbering).
- Suggested name: 'printer spec-from-followups <name> [--from <path>]'. Positional <name> is the slug (e.g. 'fix-review-cycle') used in the new spec filename. --from defaults to the most recently modified file in .printer/followups/ (or errors if none); accept an explicit path to override.
- Files to change:
  * printer/src/cli.rs — add Command::SpecFromFollowups(SpecFromFollowupsArgs) with fields: name: String, from: Option<PathBuf>, agent: AgentKind, model: Option<String>, cwd: Option<PathBuf>, permission_mode (default bypassPermissions), verbose: bool.
  * printer/src/main.rs — dispatch to new module fn.
  * printer/src/spec_from_followups.rs (new module) — implements:
      pub async fn spec_from_followups(args: SpecFromFollowupsArgs) -> Result<()>
    1. Resolve cwd. If args.from is None, scan .printer/followups/ for the newest *.md (mtime). Bail with helpful message if none.
    2. Compute the destination path via shared helper next_numbered_spec_path(cwd, name) (also used by T-011) — produces 'specs/NNN-<name>.md' where NNN is one greater than the highest existing 'NNN-' prefix in specs/, zero-padded to 3.
    3. Read the followups file body.
    4. Build a one-turn agent prompt (new fn in prompts.rs: spec_from_followups_prompt(out_path, followups_body)) instructing the agent to:
       - Read the followups verbatim block included in the prompt
       - Write a clean canonical spec to <out_path> (use the existing template style: H1 title, short description paragraph, '## Tasks' with '- [ ]' bullets, optional 2-space-indented descriptions)
       - Do NOT edit any other files; do NOT execute the work
       - Emit SENTINEL_PLAN_READY when done
    5. Run a single Session::turn() with the prompt; check for SENTINEL_BLOCKED and SENTINEL_PLAN_READY; verify the file was actually created (std::fs::metadata).
    6. Print 'wrote <path>; next: printer plan <path>' on success.
  * printer/src/prompts.rs — add spec_from_followups_prompt(out_path: &str, followups: &str) -> String. Mirror init.rs::template structure in the example shown to the agent.
- Reuse: import next_numbered_spec_path from init.rs (T-011) — both commands share that helper, so put it in a small new module like printer/src/specs_paths.rs and have both call it.
- Tests:
  * Unit test for next_numbered_spec_path: empty specs/, gaps, non-conforming files all skipped, picks max+1.
  * Integration-style: not strictly required given agent-driven; rely on a manual 'cargo run -- spec-from-followups my-feature' against a saved followups file.
- Edge cases:
  * If specs/ doesn't exist, create it.
  * If the destination file already exists, bail unless --force (mirror init).
  * If --from is given and missing, bail with the path.
- Verification: cargo build, then run on the existing .printer/followups/003-followups.md (after T-009 has produced one) and confirm specs/004-<name>.md exists with valid structure that 'printer plan' can parse.
- 2026-04-29T03:43:33Z [sarocu] Implemented: cli::SpecFromFollowupsArgs + Command variant; new module spec_from_followups.rs; spec_from_followups_prompt in prompts.rs. Resolves cwd, picks newest .printer/followups/*.md (or --from), uses next_numbered_spec_path from T-011, runs one Session::turn() with the canonical-spec instructions, verifies SENTINEL_PLAN_READY + file existence. 3 unit tests for latest_followups; full suite green (60 tests). CLI verified: 'printer spec-from-followups --help'.
