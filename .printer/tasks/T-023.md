+++
id = "T-023"
title = "encourage direct click testing of projects with a desktop or web app component"
status = "done"
priority = 3
created_at = "2026-05-02T22:03:21Z"
updated_at = "2026-05-02T22:12:56Z"
owner = "sarocu"
labels = []
depends_on = ["T-021", "T-022"]
blocked_reason = ""
spec_anchor = "06bb376ebe8b5aa4"
+++
## Notes

- 2026-05-02T22:05:59Z [sarocu] Plan — push agents past unit tests into actual click-through verification for UI/web work.

Prompt edits (printer/src/prompts.rs)
- `render_review_body` (around lines 405–411): expand the "verify the change actually works" step. After "build/test/lint commands", add a sentence: "If the change touches a desktop UI or web app, do not stop at tests — exercise the running app end-to-end. Start the app (or `computer browse <local URL>` for a web app), then use the `computer` skill to drive the relevant flow and capture before/after screenshots as evidence in the report." Keep the existing read-only-during-review caveat (input synthesis is allowed because the app, not the repo, is being mutated).
- `render_nudge_body` (impl-side per-turn nudge, lines 233+): add an analogous bullet near the "tests" guidance — when a task has a UI/web surface, the agent should run the app it just changed and click through the affected flow before marking the task done. Reference the `computer` skill by name so the nudge stays self-contained.
- `## Verification` reporting hint in the review body: ask reviewers to list the click-test steps they actually performed (or explicitly say "no UI surface" / "no display available") so absence of UI verification is visible, not silent.

Skill copy (cross-checks T-022)
- plugins/computer/skills/computer/SKILL.md "When to use" section: add a paragraph that this is the canonical answer to "did I actually verify the UI?" — the prompt and the skill should agree.

Detection / fallbacks
- No code-level auto-detection of "is this a UI project" — the prompt simply tells the agent to make the call from the diff and the spec. Agents skip the click-test on backend-only diffs.
- If `$WAYLAND_DISPLAY` is unset in the sandbox, the agent should report this explicitly in the verdict instead of silently skipping (the skill already documents this; the prompt change just makes it a required call-out).

Verification
- `cargo build -p printer && cargo test -p printer` — prompt-string tests (if any) still pass; otherwise eyeball `cargo run -p printer -- review …` rendered prompt for the new wording.
- Manual: run a UI-touching spec end-to-end and confirm the review report now contains a `Click-test` line (or an explicit "no UI surface" / "no display" note).

Depends on T-021 and T-022 (refers to `computer browse` and the broadened skill description).
- 2026-05-02T22:12:56Z [sarocu] Done. prompts.rs edits: render_review_body now mandates UI/web end-to-end exercise (computer browse + click-test) and an explicit 'no display available' fallback line, and the ## Verification section asks reviewers to list click-test steps performed; render_nudge_body now tells impl agents to run the app they just changed before marking UI/web tasks done. cargo build/test pass (126 tests).
