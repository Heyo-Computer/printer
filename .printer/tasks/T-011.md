+++
id = "T-011"
title = 'when using "init" to create a spec doc, if .printer already exists (e.g. printer has been used in the repo before) then create a specs/ folder and use a numbered system for a naming template, e.g. "001-cool-feature.md" and "002-followup.md" where the user passes in the name to be used, like "printer init feat-deploy-assets"'
status = "done"
priority = 3
created_at = "2026-04-29T03:34:25Z"
updated_at = "2026-04-29T03:41:51Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "1392d931e84af86a"
+++
## Notes

- 2026-04-29T03:36:44Z [sarocu] Plan:
- Goal: when 'printer init' runs in a repo where .printer/ already exists, treat the positional argument as a slug name (not a path) and write the spec to specs/NNN-<slug>.md with NNN auto-incremented.
- Behavior matrix (printer init [arg] [--title T] [--force]):
  * .printer/ does NOT exist: keep current behavior — arg is treated as a path (default 'spec.md').
  * .printer/ DOES exist: arg is treated as a slug. Resolve target = specs/<NNN>-<slug>.md. Refuse if no slug supplied (clear error: 'pass a slug like printer init feat-deploy-assets'). Validate slug matches /^[a-zA-Z0-9._-]+$/ to avoid path traversal / spaces.
  * --force still controls overwrite of an existing target file.
- Existing specs in this repo (specs/001-improve-review.md, 002-planning-stages.md, 003-followups.md) confirm the desired NNN-<slug> pattern; new code should pick 004 next.
- Files to change:
  * printer/src/cli.rs InitArgs — change 'path: Option<PathBuf>' to 'name: Option<String>'. Update doc comment to describe both modes. (Path-as-arg compatibility for non-printer repos: still accept a string, then InitArgs::path_or_slug() decides.)
  * printer/src/init.rs init():
    - Resolve project_root first.
    - Detect printer_dir = root.join('.printer'). If exists: branch B (slug mode). Else: branch A (path mode).
    - Branch A: arg as PathBuf or default 'spec.md' (current behavior preserved).
    - Branch B:
        let slug = args.name.context('repo already initialized; pass a slug like printer init feat-deploy-assets')?;
        validate_slug(&slug)?;  // alnum + . _ -
        let path = next_numbered_spec_path(&root, &slug)?;
        std::fs::create_dir_all(root.join('specs'))?;
        // existence + --force handling unchanged
    - Write template, then call bootstrap_printer_dir() (idempotent since .printer/ exists; just ensures tasks dir).
    - Emit a tip 'Next: printer plan <path>' instead of 'printer run' to align with current default flow (planning is now mandatory).
  * printer/src/specs_paths.rs (new — shared with T-010): next_numbered_spec_path(root, slug) -> PathBuf scanning root/specs/ for entries matching ^([0-9]{3})-.*\.md$, returning specs/<max+1 padded to 3>-<slug>.md. Start at 001 if dir empty/missing.
- Tests (in init.rs or specs_paths.rs):
  * next_numbered: empty -> 001-foo.md; with 001/002/003 present -> 004-foo.md; ignores non-NNN-*.md files; ignores subdirs.
  * validate_slug: rejects '..', '/', spaces; accepts 'feat-deploy_assets.v2'.
  * init_existing_printer_uses_slug: in a tempdir with .printer/ pre-created, calling init(slug='foo') writes specs/001-foo.md.
  * init_no_printer_uses_path: in tempdir without .printer/, calling init(path='spec.md') writes spec.md (legacy behavior).
- Docs to touch:
  * printer/README.md — update the init usage section to document the two modes (briefly).
- Verification: cargo test, then manual 'printer init my-feature' in a repo with .printer/ vs without; check generated paths.
- 2026-04-29T03:41:51Z [sarocu] Implemented: new specs_paths module (next_numbered_spec_path + validate_slug). init.rs: branches on .printer/ existence — slug mode writes specs/NNN-<slug>.md, legacy mode keeps path semantics. README updated. 11 new tests, full suite green (57 passing).
