+++
id = "T-013"
title = 'the initial and default plugin will be the "heyvm" cli https://docs.heyo.computer'
status = "done"
priority = 3
created_at = "2026-04-30T16:02:48Z"
updated_at = "2026-04-30T16:36:21Z"
owner = "sarocu"
labels = []
depends_on = ["T-012", "T-016"]
blocked_reason = ""
spec_anchor = "d434ad29820d8702"
+++
## Notes

- 2026-04-30T16:04:29Z [sarocu] ## Plan: heyvm as default driver

Scope: make `heyvm` the registry default + first-class driver entry. The registry already lists it (`printer/src/plugins/registry.rs:24`); this task wires it to the driver framework from T-012 so a fresh user can `printer add-plugin heyvm` and immediately get sandboxing.

### Steps

- Confirm `printer/src/plugins/registry.rs` `KnownInstaller::Shell { command, binary }` for `heyvm` is still correct (URL: https://heyo.computer/heyvm/install.sh, binary `~/.local/bin/heyvm`). If the install script no longer exists, fall back to a `Cargo { git }` variant once heyvm is open-sourced — leave a TODO note in registry.rs but do not change the URL speculatively.
- Add a `default_driver` notion in the global config (T-014): when the config is missing or `driver = "auto"`, printer picks `heyvm` if it's the only installed driver. Document this in `HOOKS.md`.
- Update `printer/HOOKS.md` "Authoring a plugin" section: add a worked `[driver]` example pointing at heyvm worktree commands (`heyvm worktree create`, `heyvm worktree exec <id> -- <cmd>`, `heyvm worktree destroy <id>`).
- Update `printer/src/plugins/store.rs::prompt_if_no_plugins` copy: mention that installing `heyvm` enables sandboxing by default, so users understand the upsell.

### Verification

- `printer add-plugin heyvm` on a clean `~/.printer/plugins/` resolves via registry, drops the manifest, and `printer plugins` lists it with a `driver=vm` indicator (add to `list_installed` printout in `store.rs:171`).
- `cargo test -p printer` (registry lookup test).
- Manual: `printer hooks list` is unchanged; new `printer drivers list` (cheap addition, optional — call it out as a follow-up if not landing in T-012).

### Depends on

- T-012 (driver framework must exist before this can wire heyvm in).
- T-016 ships the actual `[driver]` block; this task just promotes heyvm to the default.
- 2026-04-30T16:32:20Z [sarocu] ## Refreshed plan (deps now landed; some items are already done)

Status of the earlier plan items:
- **default_driver in global config**: already done — T-014 added `SandboxDriverChoice::Auto` which picks the single installed driver. No further work.
- **HOOKS.md worked heyvm example**: already in HOOKS.md (lines ~237–253 and ~321–325). No further work.
- **`printer plugins` shows driver role**: already done — T-012 added the ROLES column.

What's left for this task:

- Verify the registry entry at `printer/src/plugins/registry.rs:24` still resolves: `curl -fsSL https://heyo.computer/heyvm/install.sh | sh` — fetch HEAD on that URL. If it 404s, leave the URL alone but add a TODO note at `registry.rs:23` pointing users at `printer add-plugin path:plugins/heyvm` (T-016 ships the bundled copy) until the install script is public.
- Update `printer/src/plugins/store.rs::prompt_if_no_plugins` (lines 113–127): extend the "Install one with…" line to mention that installing `heyvm` enables sandboxing by default (one-sentence upsell, kept generic — don't hard-code heyvm in the error path).
- Cross-link from `plugins/heyvm/README.md` (created in T-016) back to the registry name once it's confirmed working.

### Verification

- `printer add-plugin heyvm` on a clean `~/.printer/plugins/` either resolves via registry (success path) or prints a clear error pointing at the path: form (TODO path).
- `printer run` / `printer exec` with no plugins installed prints the updated upsell copy.
- `cargo test -p printer` still green (registry::lookup test unchanged unless URL is changed).
- 2026-04-30T16:36:21Z [sarocu] Implemented per refreshed plan.

Registry URL check: https://heyo.computer/heyvm/install.sh returns HTTP 200 — left registry.rs:24 alone (no TODO needed). Inspecting the script confirms it lands the binary at ~/.local/bin/heyvm when /usr/local/bin is not writable, which matches registry.rs:28 ('binary = ~/.local/bin/heyvm').

Changes:
- printer/src/plugins/store.rs::prompt_if_no_plugins (lines ~122–128): extended the upsell copy to mention that a driver-contributing plugin (e.g. heyvm) enables sandboxing by default, while still pointing users at the generic 'printer add-plugin <name>' form. Verified by HOME-overriding to an empty dir and running printer exec — message renders as expected.
- plugins/heyvm/README.md: rewrote the Install section. Honest docs on the registry-vs-path: split — registry name installs only the heyvm CLI binary (shell installer has no source_dir, so install.rs at line 49 skips printer-plugin.toml), so the driver+skill manifest currently needs 'printer add-plugin path:plugins/heyvm --force' as a second step. Documented this two-step workflow.

Verification:
- cargo build/test -p printer: 88 passed.
- HOME=/tmp/printer-empty-home printer exec — new upsell line prints.
- printer plugins still shows heyvm with ROLES=hooks+driver from T-016.
