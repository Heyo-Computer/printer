+++
id = "T-028"
title = "spawn an agent to create a plan and then execute for each task"
status = "done"
priority = 3
created_at = "2026-05-05T16:36:27Z"
updated_at = "2026-05-05T17:26:57Z"
owner = "sarocu"
labels = []
depends_on = ["T-027", "T-029"]
blocked_reason = ""
spec_anchor = "04553e95cec62843"
+++
## Notes

- 2026-05-05T16:39:09Z [sarocu] ## Plan

- Modify : add recursive loop that iterates over tasks
- For each task: spawn agent with task-specific spec, create plan first, then execute
- Use existing  infrastructure
- Files: exec.rs (main loop), tasks/cli.rs (task parsing for iteration)
- Verification:  in printer
- 2026-05-05T17:04:29Z [sarocu] Already implemented:
- exec_for_task() in exec.rs already runs the planning phase first via run_action()
- run_action() with Action::Fresh calls do_run_then_review() which runs planning then implementation
- The codegraph watch daemon is spawned per-worktree
- The heyvm worktree/sandbox is created per-task
- Dependencies are honored via compute_ready()

The recursive exec flow is complete. Only needs testing.
- 2026-05-05T17:14:00Z [sarocu] PARTIAL: The code runs `run_action` which includes planning and execution via `do_run_then_review`, but this runs in the current process context rather than spawning a separate agent per task. Need actual agent spawning per task (subprocess isolation) rather than sequential execution in the same process.
- 2026-05-05T17:26:57Z [sarocu] Review finding: PARTIAL - exec_for_task does spawn a printer subprocess for each task (exec.rs:582-604), which will run planning and execution. However, this is a subprocess call to the same printer binary, not a separate agent instance with isolated worktree/sandbox as spec requires.
