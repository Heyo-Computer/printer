+++
id = "T-009"
title = "save the followups from a review agent in the .printer dir for that spec"
status = "done"
priority = 3
created_at = "2026-04-29T03:34:25Z"
updated_at = "2026-04-29T03:40:13Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "ec8eef6e2dfe3d9c"
+++
## Notes

- 2026-04-29T03:36:03Z [sarocu] Plan:
- Goal: persist the 'Suggested follow-ups' content emitted by each review pass to a stable location under .printer/, keyed by the spec, so a future 'printer spec-from-followups' (T-010) can consume it.
- Save location: .printer/followups/<spec-stem>.md (e.g. specs/003-followups.md -> .printer/followups/003-followups.md). Choose the spec file stem so re-reviews of the same spec overwrite the same file. Create dir on demand.
- File format (markdown, written by printer not the agent):
  * H1 with spec path and timestamp
  * 'Verdict: PASS|PARTIAL|FAIL' line
  * Verbatim copy of the report's '## Suggested follow-ups' section (parsed). If absent, write 'none'.
  * Optionally append the 'Out-of-scope or risky changes' section as a second block — keep simple: just suggested follow-ups for now.
- Where to wire it (files to change):
  * printer/src/review.rs — extend ReviewOutcome with a parsed 'followups: Option<String>' (or just keep the raw report and parse in caller). Cleanest: add a small helper fn extract_section(report, '## Suggested follow-ups') -> Option<String> in review.rs, plus tests mirroring parse_verdict tests.
  * printer/src/exec.rs do_review() — after each review pass, write the followups file via a new helper write_followups(cwd, spec, &outcome). Overwrite each pass so the final pass wins. Use the cwd already resolved in exec().
  * Note: review.rs's standalone 'printer review' path doesn't have an exec checkpoint or canonical cwd; mirror the same write there using std::env::current_dir() (or the --cwd) so 'printer review' alone also produces the file. Reuse the helper.
- Helper signature (suggest in exec.rs or new printer/src/followups.rs module):
    pub fn write_followups(cwd: &Path, spec_abs: &Path, verdict: Verdict, body: &str) -> Result<PathBuf>
  Returns the path written, for logging.
- Section-extraction parser:
  * Find first occurrence of '## Suggested follow-ups' (case-insensitive on the header).
  * Take lines until the next '## ' header or EOF; trim.
  * Return None if absent or body trims to empty/'none'.
- Tests:
  * extract_section: present-with-bullets, missing, empty, 'none' -> None.
  * write_followups: round-trips file contents into the expected path.
- Verification: cargo test in printer/, then 'printer review specs/003-followups.md --out /tmp/r.md' and assert .printer/followups/003-followups.md appears with the parsed section.
- 2026-04-29T03:40:13Z [sarocu] Implemented: extract_section + write_followups in review.rs; called from review::review() so both 'printer review' and exec do_review() persist to .printer/followups/<spec-stem>.md. 5 new tests pass; full suite green (46 tests).
