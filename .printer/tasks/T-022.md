+++
id = "T-022"
title = "update skills so that agents are aware of the computer cli's testing ability"
status = "done"
priority = 3
created_at = "2026-05-02T22:03:21Z"
updated_at = "2026-05-02T22:12:14Z"
owner = "sarocu"
labels = []
depends_on = ["T-021"]
blocked_reason = ""
spec_anchor = "fe21350deac957d2"
+++
## Notes

- 2026-05-02T22:05:44Z [sarocu] Plan — surface the computer CLI as a *testing* tool, not just a review-evidence tool.

Skill content edits
- plugins/computer/skills/computer/SKILL.md
  - Frontmatter `description`: add "…and to drive desktop or web apps end-to-end during implementation or review (open URLs, click through flows, capture before/after screenshots)." So the auto-loader picks it up for impl turns too, not just review.
  - Section "When to use this skill": add an "Implementation-mode use" sub-section that explicitly OKs input synthesis when verifying a change you just made (mirrors the existing review-mode read-only stance).
  - Section "Command surface": add the `browse <URL>` row from T-021 once it lands; add a "Quick decision table" entry: `Open a web app for click-testing → computer browse https://localhost:3000`.
  - Add a short "End-to-end click testing" recipe to "Composing reliable observations": launch app/URL → `sleep` → `windows --json` to confirm it's up → `screenshot before.png` → drive input → `screenshot after.png` → diff/report.
- plugins/computer/skills/computer/README.md — mirror the new examples (browse + click-test recipe).

Plugin wiring
- plugins/computer/printer-plugin.toml — add a second `[[hooks]]` entry with `event = "before_run"` so the skill is also injected on impl turns (currently only `before_review`). Keep the existing `before_review` hook.

Printer prompt copy
- printer/src/prompts.rs::SKILLS_HEADER — broaden the wording past "verification" to also cover "exercising the change you just made" so impl-mode agents understand they may use UI-driving skills, not just static checks. One-line tweak; do not duplicate skill descriptions.

Verification
- `cargo build -p printer && cargo test -p printer` (skills.rs frontmatter tests still pass after description change).
- Reinstall the computer plugin in a scratch checkout; confirm SKILL.md ends up under both `before_run` and `before_review` skill assets.
- Spot-check by running `printer exec` on a tiny dummy spec touching a UI: the rendered nudge should now list `computer` as an available skill on impl turns.

Depends on T-021 (the new `browse` verb is referenced from the skill copy).
- 2026-05-02T22:12:14Z [sarocu] Done. SKILL.md frontmatter description broadened to cover impl + review modes; added 'Implementation-mode use' subsection and an 'End-to-end click-test recipe'. README already had the click-test section from T-021. printer-plugin.toml now wires both before_run and before_review hooks for skills/computer/SKILL.md. prompts.rs SKILLS_HEADER reworded past 'verification' to 'exercising or verifying the change you just made'. cargo build/test pass (126 tests).
